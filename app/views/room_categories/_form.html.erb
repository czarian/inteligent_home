<%= form_for(@room_category) do |f| %>
  <% if @room_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room_category.errors.count, "error") %> prohibited this room_category from being saved:</h2>

      <ul>
      <% @room_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= @room_category.room.name %> - <%= @room_category.category.name %>
  </div>

   <div class="field">
   <h4> Devices: </h4>

    <%= f.fields_for :devices do |d| %>
     <%= d.label :name, "Name" %>        
     <%= d.text_field :name %>
    <br>
     <%= d.label :type, "Type" %>
      <%= d.select :type, Device::TYPES  %>
    <% end%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>