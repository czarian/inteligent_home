<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @room.name %>
</p>
<p>
  <strong>Categories:</strong>
  <% @room.categories.each do |cat| %>
  	  <strong><%= cat.name %></strong>
  	  <% end %>
</p>

<%= link_to 'Edit', edit_room_path(@room) %> |
<%= link_to 'Back', rooms_path %>